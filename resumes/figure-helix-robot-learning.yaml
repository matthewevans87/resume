# Per-route override for Figure — Helix AI Engineer, Robot Learning (San Jose, CA).
# Route slug: figure-helix-robot-learning
# Changes vs. base:
#   1. personal.title set to "Helix AI Engineer, Robot Learning".
#   2. skills_highlight hidden (subsumed by qualifications block).
#   3. qualifications block mapped to Figure's JD: Requirements first
#      (visuomotor manipulation, behavior cloning + RL, Python, PyTorch,
#      experimentation/failure-analysis, classical-vs-learning tradeoffs,
#      ambiguity/ownership), then Bonus items anchored to base content
#      (production deployment, leading projects).
#      Real-robot deployment is framed honestly: sim-to-real pipeline targeting
#      transfer, with the SO-101 hardware as the deployment target.
#      Publications, humanoid hardware experience intentionally omitted.

personal:
  title: "Helix AI Engineer, Robot Learning"

skills_highlight:
  disabled: true

qualifications:
  title: "Qualifications"
  items:
    - "Visuomotor manipulation policies: End-to-end ML pipeline training vision-based perception and control policies for a wrist-camera SO-101 manipulation task in NVIDIA Isaac Sim under domain randomization (lighting, camera pose, object geometry, distractors) targeting zero-shot sim-to-real transfer — 89.7% task success"
    - "Behavior cloning & reinforcement learning: PPO with phased reward shaping (approach → grasp → lift) and safety-aware termination; task-specific CNN bootstrapped from synthetic policy rollouts via iterative self-distillation, replacing a frozen ResNet18 backbone (+4.5pp success rate, 87.1% vs. 83.1%)"
    - "Robustness to real-world challenges: Domain randomization across lighting, camera pose, object geometry, and distractors; multimodal observation handling across RGB imagery, joint-state proprioception, and object-pose telemetry"
    - "Python & PyTorch: Daily driver across vision-based RL (PPO via skrl), LSTM motion-planning, and 21-variant transformer language-modeling project; NumPy, SciPy, TensorBoard"
    - "Experiment design & failure analysis: Sweep runner for Cartesian-product ablations across reward functions, vision backbones, and PPO hyperparameters (5 RNG seeds each); leave-one-out / leave-one-in reward analyses identified the dense absolute approach reward as a soft local optimum — removing it yielded the best result observed (+2pp over the 13-term baseline) with a derived 2-term minimal reward matching the full set within noise (Welch z = -0.47)"
    - "Classical vs. learning-based tradeoffs: ML-assisted visibility-graph construction for shortest-path motion planning — trained an LSTM sector predictor to gate visibility checks, reducing vertex comparisons up to 98% vs. exact construction while closely matching shortest-path length"
    - "Ownership in ambiguous environments: 15 yrs production software shipping greenfield systems end-to-end; sole research engineer / author across four graduate ML projects; self-directed 15-week neural networks independent study (paper survey → from-scratch PyTorch implementations → thesis proposal)"
    - "Production deployment of complex ML systems: Architected an event-driven ETL platform on AWS streaming terabyte-scale CMS Transparency in Coverage data across thousands of concurrent Lambda + ECS Fargate workers feeding a multi-tenant SaaS"
    - "Reliable, well-tested software: 15 yrs of production code; CI/CD pipelines (GitHub Actions, Azure DevOps YAML), peer code review, typed YAML config validation, versioned artifacts, provenance tracking"
    - "Leading projects & teams: Associate Partner at Zeal IT Consultants — led architecture and delivery of cloud-native SaaS products through full SDLC across multiple concurrent client accounts in healthcare, financial services, and benefits administration"
